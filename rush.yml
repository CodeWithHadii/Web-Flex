name: WebFlex
description: A simple App Inventor extension to dynamically load HTML and CSS content into a WebViewer component.

version:
  number: auto
  name: '1.0'

authors:
  - Hadi Ch

build:
  release:
    optimize: true

  desugar:
    enable: false
    desugar_deps: false

assets:
  icon: icon.jpeg
